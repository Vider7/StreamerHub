# Normalize line endings; keep Windows scripts working when cloned anywhere.
* text=auto

*.bat text eol=crlf
*.ps1 text eol=crlf
*.sln text eol=crlf
*.csproj text eol=crlf

# Never corrupt binary helpers if any land in the tree.
*.exe binary
*.dll binary
*.7z binary
*.zip binary
